class ArticalModel {
  final String title;
  final String ? description;
  final String ? imageUrl;

  ArticalModel({
    required this.title,
    required this.description,
    required this.imageUrl,
  });

}